<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} - {% endif %}Icon Offerer</title>
    <meta name="description" content="{{ description or 'Сервис для подбора иконок по словесному запросу' }}">
    <link rel="stylesheet" href="{{ baseUrl }}/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
</head>
<body>
    <header class="header">
        <nav class="nav">
            <a href="{{ baseUrl }}/" class="nav__logo">icon-offerer</a>
        </nav>
    </header>

    <main class="main">
        {{ content | safe }}
    </main>

    <footer class="footer">
        <p>icon-offerer — подбор иконок по словесному запросу</p>
    </footer>

    <script src="{{ baseUrl }}/js/main.js"></script>
</body>
</html>
