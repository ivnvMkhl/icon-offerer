---
layout: base.njk
title: Ant Design Icons - AI поиск иконок по описанию | Копирование кода иконки
description: AI-поиск иконок Ant Design по описанию на русском языке. Найти иконку antd по словесному запросу, копирование кода иконки, превью иконок Ant Design онлайн
---

{% block content %}
<div class="container">
    <h1>AI Поиск иконок Ant Design по описанию</h1>
    
    <div class="search-section">
        <div class="search-container">
            <input 
                type="text" 
                id="iconSearch" 
                class="search-input" 
                placeholder="Найти по имени или вопрос AI"
                autocomplete="off"
            >
            <button id="aiSearchButton" class="ai-search-button">
                <span class="ai-search-text">Спросить AI</span>
            </button>
        </div>
    </div>
    
    <!-- Контейнер для AI сообщений -->
    <div id="aiMessagesContainer" style="display: none;"></div>
    
               <div id="iconsContainer">
               <div class="loading-indicator" id="loadingIndicator">
                   <div class="loading-spinner"></div>
                   Загрузка иконок...
               </div>
           </div>
    
    <div class="no-results" id="noResults" style="display: none;">
        <p>Иконки не найдены. Попробуйте изменить поисковый запрос.</p>
    </div>
</div>

<script>
// Загружаем данные иконок и генерируем HTML
fetch('{{ baseUrl }}/js/antd-icon-paths.json')
    .then(response => response.json())
    .then(iconPaths => {
        const iconsGrid = document.getElementById('iconsGrid');
        const resultsCount = document.getElementById('resultsCount');
        
        // Генерируем HTML для всех иконок
        const iconsHtml = Object.keys(iconPaths).map(iconName => `
            <div class="icon-card" data-name="${iconName}" data-keywords="">
                <div class="icon-preview">
                    <div class="icon-placeholder" data-icon="${iconName}">
                        <!-- Иконка будет загружена через JavaScript -->
                    </div>
                </div>
                <div class="icon-info">
                    <div class="icon-name">${iconName}</div>
                </div>
                <div class="icon-actions">
                    <button class="copy-btn" data-icon="${iconName}" title="Копировать название">
                        <span class="copy-text">Копировать</span>
                    </button>
                </div>
            </div>
        `).join('');
        
        iconsGrid.innerHTML = iconsHtml;
        resultsCount.innerHTML = `Найдено иконок: <strong>${Object.keys(iconPaths).length}</strong>`;
        
        // Инициализируем функциональность после загрузки иконок
        if (typeof initAntdIcons === 'function') {
            initAntdIcons(iconPaths);
        }
    })
    .catch(error => {
        console.error('Ошибка загрузки иконок:', error);
        document.getElementById('resultsCount').innerHTML = 'Ошибка загрузки иконок';
    });
</script>

<script>
    // Устанавливаем глобальную переменную baseUrl для JavaScript
    window.baseUrl = '{{ baseUrl }}';
</script>
<script src="{{ baseUrl }}/js/ai-search.js"></script>
<script src="{{ baseUrl }}/js/paginated-icons.js"></script>
<script src="{{ baseUrl }}/js/antd-icons.js"></script>
{% endblock %}